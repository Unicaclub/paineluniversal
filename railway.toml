# Railway Configuration
# Defines how to deploy each service

# Frontend Service
[services.frontend]
build.dockerfile = "frontend/Dockerfile"

# Backend Service (using simple Dockerfile for reliability)
[services.backend]
build.dockerfile = "backend/Dockerfile.simple"

# MEEP Service
[services.meep-service]
build.dockerfile = "meep-service/Dockerfile"
